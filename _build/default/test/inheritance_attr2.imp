var circle_descr;
var point_descr;
var carre_descr;

function main() {
  var p;
  var p2;
  var c;
  var carre;
  
  p = alloc(16);
  *p = *point_descr;
  point_constructor(p, 1, 2);
  p2 = alloc(16);
  *p2 = *point_descr;
  point_constructor(p2, 5, -5);
  c = *(*(*p + 16))(p, 48);
  carre = alloc(16);
  *carre = *carre_descr;
  carre_constructor(carre, 5);
  putchar(*(*(*p2 + 4))(p2));
  putchar(*(*(*p + 4))(p));
  putchar(*(*(*carre + 4))(carre));
  putchar(c);
}

function circle_get_z(_this) {
  
  return((*(_this + (4 * 1)) + 97));
}

function circle_test(_this) {
  
  return(1);
}

function point_constructor(_this, x, y) {
  
  *(_this + (4 * 2)) = x;
  *(_this + (4 * 3)) = y;
  *(_this + (4 * 1)) = (x + y);
}

function point_sum(_this, x) {
  
  return(((x + *(_this + (4 * 2))) + *(_this + (4 * 3))));
}

function carre_constructor(_this, x) {
  
  *(_this + (4 * 2)) = x;
  *(_this + (4 * 3)) = x;
  *(_this + (4 * 1)) = x;
}


